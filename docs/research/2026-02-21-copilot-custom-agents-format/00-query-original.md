# Original Query

## User Query

GitHub Copilot custom agents configuration format 2025 2026 - .agent.md file format, YAML frontmatter specification, tools configuration, custom agents in VS Code, GitHub Copilot CLI agents, .github/agents/ directory structure, copilot-instructions vs custom agents

## Inferred Context

```json
{
  "focus": "technical",
  "temporal": "recent",
  "domain": ["GitHub Copilot", "VS Code", "CLI", "YAML", "Markdown"],
  "skip_clarification": true
}
```

## Motivation

Issue #138 reports that agent files generated by `npx aios-core install` in `.github/agents/` are not recognized by GitHub Copilot because they use Claude Code format instead of the Copilot custom agents specification.
